import 'package:flutter/material.dart';
import 'package:tap_demo/design_system/atoms/sizes.dart';

const microBorderRadius = BorderRadius.all(Radius.circular(micro));
const tinyBorderRadius = BorderRadius.all(Radius.circular(tiny));
const smallBorderRadius = BorderRadius.all(Radius.circular(small));
const mediumBorderRadius = BorderRadius.all(Radius.circular(medium));
const largeBorderRadius = BorderRadius.all(Radius.circular(large));
